import numpy as np
import random
def kukold(x,y):
    if len(x) != len(y):
        print('idi n***i')
    else:
        a = len(x)*sum(x*) - sum(x)*sum(y)
    print(a)
r = np.random
t = np.random
kukold(r,t)